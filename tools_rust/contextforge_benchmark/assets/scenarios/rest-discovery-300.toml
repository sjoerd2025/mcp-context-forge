[suite]
name = "benchmark-gunicorn-rest-discovery-compare"
description = "Exercises the REST discovery surfaces for servers, resources, and prompts with matched load settings, then compares the baseline benchmark image against the rust-plugin benchmark image to show whether catalog-style discovery traffic changes under the Rust extension."
output_root = "reports/benchmarks"
continue_on_failure = false
save_intermediate_artifacts = true
flamegraph_enabled = false

[defaults.setup]
target_kind = "gateway"
auth_mode = "jwt"
plugins_enabled = false

[defaults.build]
rust_plugins = false
profiling_image = false
container_file = "tools_rust/contextforge_benchmark/assets/Containerfile"
image_name = "mcpgateway/mcpgateway"
image_tag = "benchmark-suite-rest-discovery"
rebuild_policy = "missing"

[defaults.runtime]
http_server = "gunicorn"
host = "127.0.0.1"
transport_type = "streamablehttp"

[defaults.runtime.gunicorn]
workers = 12
timeout = 30
graceful_timeout = 30
keep_alive = 10
max_requests = 0
max_requests_jitter = 0
backlog = 16384
preload_app = true
dev_mode = false

[defaults.gateway]
disable_access_log = true
templates_auto_reload = false
structured_logging_database_enabled = false
sqlalchemy_echo = false
log_level = "ERROR"

[defaults.load]
driver = "contextforge_goose"
headless = true
only_summary = true
html_report = false
users = 300
spawn_rate = 60
run_time = "180s"
target_service = "nginx"

[defaults.load.workload]
fallback_endpoint = "/servers"

[defaults.load.workload.endpoints."/health"]
enabled = false

[defaults.load.workload.endpoints."/ready"]
enabled = false

[defaults.load.workload.endpoints."/admin/plugins"]
enabled = false

[defaults.load.workload.endpoints."/servers"]
enabled = true
weight = 6

[defaults.load.workload.endpoints."/resources"]
enabled = true
weight = 5

[defaults.load.workload.endpoints."/prompts"]
enabled = true
weight = 5

[defaults.load.workload.endpoints."/mcp tools/list"]
enabled = false

[defaults.load.workload.endpoints."/mcp resources/list"]
enabled = false

[defaults.load.workload.endpoints."/mcp prompts/list"]
enabled = false

[defaults.measurement]
warmup_seconds = 30
measure_seconds = 120
profile_seconds = 0
cooldown_seconds = 30

[defaults.profiling]
enabled = false
tools = ["perf", "flamegraph"]
duration_seconds = 0
required = false

[defaults.execution]
retry_enabled = true
max_attempts = 2
capture_logs = true
save_raw_results = true
reuse_stack = true

[[scenario]]
name = "gunicorn-rest-discovery-baseline"
description = "REST discovery benchmark on the baseline benchmark image"
scenario_type = "rest_discovery_baseline"

[scenario.build]
rust_plugins = false
image_tag = "benchmark-suite-rest-discovery-baseline"

[[scenario]]
name = "gunicorn-rest-discovery-rust-plugins"
description = "REST discovery benchmark on the rust-plugin benchmark image"
scenario_type = "rest_discovery_rust_plugins_compare"

[scenario.build]
rust_plugins = true
image_tag = "benchmark-suite-rest-discovery-rust"
